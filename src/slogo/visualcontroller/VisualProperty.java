package slogo.visualcontroller;

/**
 * A visual property enum, to ensure property integrity
 * @auther Max Smith
 */
public enum VisualProperty {
    COMMAND, DATA, VARIABLE, ERROR, FUNCTION, FILE, TURTLE;
}
